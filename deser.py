import glob

# Process all observation tables
for file in glob.glob('observation-tables*.csv'):
    # Perform operations on each file
    print(f"Processing {file} ...")
    # Add your processing logic here
